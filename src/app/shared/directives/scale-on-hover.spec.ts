import { ScaleOnHover } from './scale-on-hover';

describe('ScaleOnHover', () => {
  it('should create an instance', () => {
    const directive = new ScaleOnHover();
    expect(directive).toBeTruthy();
  });
});
